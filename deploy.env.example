# Copy to deploy.env (gitignored) for npm run deploy / quick-deploy / watch-webrtc-logs.
DEPLOY_HOST=192.168.1.50
DEPLOY_USER=bridge
DEPLOY_DIR=/opt/matter-cameras

# Log timestamps in /api/logs and docker logs (IANA name). Defaults to America/Sao_Paulo in docker-compose.yml.
# TZ=America/Sao_Paulo
