# Used with: docker compose -f docker-compose.cli.yml up -d
# Set LAN_IP to this machine's LAN IPv4 (not 127.0.0.1).

LAN_IP=192.168.1.50
REGISTRY=ghcr.io
IMAGE_OWNER=patricktd
IMAGE_TAG=latest
TZ=America/Sao_Paulo
WEB_PORT=3202
MATTER_PORT=5550
GITHUB_REPO=patricktd/MatterCameras
